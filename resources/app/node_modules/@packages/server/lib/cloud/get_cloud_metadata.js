"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCloudMetadata = void 0;
const getCloudMetadata = async (cloudDataSource) => {
    const cloudEnv = (process.env.CYPRESS_CONFIG_ENV || process.env.CYPRESS_INTERNAL_ENV || 'production');
    const cloudUrl = cloudDataSource.getCloudUrl(cloudEnv);
    const cloudHeaders = await cloudDataSource.additionalHeaders();
    return {
        cloudUrl,
        cloudHeaders,
    };
};
exports.getCloudMetadata = getCloudMetadata;
