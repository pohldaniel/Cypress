"use strict";
/* Node doesn't expose its own internal system error, so we have to kind of ducktype it */
Object.defineProperty(exports, "__esModule", { value: true });
exports.SystemError = void 0;
const SystemErrorKind = 'SystemError';
class SystemError extends Error {
    constructor(originalError, url) {
        super(originalError.message);
        this.originalError = originalError;
        this.url = url;
        this.kind = SystemErrorKind;
    }
    static isSystemError(error) {
        return (error === null || error === void 0 ? void 0 : error.kind) === SystemErrorKind;
    }
}
exports.SystemError = SystemError;
